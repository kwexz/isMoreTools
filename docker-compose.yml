services:
  ismoretools:
    build:
      context: .
      dockerfile: Dockerfile
    image: ismoretools:latest
    container_name: ismoretools
    restart: unless-stopped
    ports:
      # The public entry point is the host nginx reverse proxy.
      # Do not expose the application container directly to the Internet.
      - "127.0.0.1:8080:80"
