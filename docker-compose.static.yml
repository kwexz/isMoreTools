services:
  ismoretools:
    image: nginx:1.27-alpine
    container_name: ismoretools
    restart: unless-stopped
    ports:
      - "127.0.0.1:8090:80"
    volumes:
      - ./out:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
